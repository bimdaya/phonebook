import React from 'react';
import PropTypes from 'prop-types';
import {Grid} from 'semantic-ui-react';
import './Footer.css'

const Footer = () => (
	<Grid.Row className='grid-row-footer'>
	<p>created by Bim</p>
	</Grid.Row>
);

export default Footer;
